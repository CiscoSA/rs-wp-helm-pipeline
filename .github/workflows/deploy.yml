name: CI/CD Pipeline

permissions:
  id-token: write # This is required for requesting the JWT
  contents: read # This is required for actions/checkout

on:
  push:
    branches:
      - task_5
  pull_request:
    branches:
      - main

# env:
#   AWS_REGION : "us-east-1"
#   PUBLIC_KEY: ${{ secrets.PUBLIC_KEY }}
#   PRIVATE_KEY: ${{secrets.PRIVATE_KEY}}

jobs:
  install_k8s:
    runs-on: ubuntu-latest
    # needs: terraform_check
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: k8s-tools
        run: |
          curl -O https://get.helm.sh/helm-v3.16.2-linux-amd64.tar.gz
          tar -xf helm-v3.16.2-linux-amd64.tar.gz
          sudo mv linux-amd64/helm /usr/local/bin
          sudo chmod +x /usr/local/bin/helm
          curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: check-wp
        run: |
          helm list -n wp
          kubectl get all -n wp

      - name: check-wp
        run: |
          wget https://github.com/CiscoSA/rsschool-devops-course-tasks/raw/refs/heads/dev/Screenshots/Chart-WP-0.1.0.tgz
          [ -f wp-version ] && helm upgrade my-wp ./Chart-WP-0.1.0.tgz --set wp.image=`cat wp-version` --set namespace=wp --set mysql.pass=qwerty -n wp


      - name: check-wp
        run: |
          helm list -n wp
          kubectl get all -n wp
